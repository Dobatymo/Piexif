# Tests require Image.close() and the legacy EXIF rational tuple format.
Pillow>=2.5.0,!=3.0.*,<7.2.0
# Sphinx 1.3.1 is the oldest version tested with the documentation and theme.
Sphinx>=1.3.1
sphinx-rtd-theme
